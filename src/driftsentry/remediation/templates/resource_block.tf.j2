# Auto-generated by DriftSentry — review before applying
resource "{{ resource_type }}" "{{ resource_name }}" {
  {% for key, value in attributes.items() %}
  {{ key }} = {{ value | tojson }}
  {% endfor %}
}
